// schema-version: {{schemaVersion}}
{{&prelude}}import { describe, it, expect } from "vitest";
import { {{schemaName}} } from "{{schemaImport}}";

describe("{{schemaName}} (view_types.{{viewName}})", () => {
{{#cases}}
  it("{{name}}", () => {
    const value = {{&fixture}};
    expect(() => {{schemaName}}.parse(value)).{{assertion}}();
  });
{{/cases}}
});
