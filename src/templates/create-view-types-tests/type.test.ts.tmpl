// schema-version: {{schemaVersion}}
{{&prelude}}import { describe, it, expect } from "vitest";
import type { {{className}} } from "{{typeImport}}";
{{#isUnion}}
{{#members}}
import type { {{memberClass}} } from "{{memberImport}}";
{{/members}}
{{/isUnion}}

describe("{{className}} field accessors (view_types.{{viewName}})", () => {
{{#isShaped}}
  const sample = (): {{className}} => ({{&fixture}});

  it("constructs a sample", () => {
    expect(sample()).toBeDefined();
  });

{{&fieldTests}}{{/isShaped}}{{#isUnion}}
{{#members}}
  it("accepts a {{name}} member", () => {
    const value: {{className}} = {{&memberFixture}} as {{memberClass}};
    expect(value).toBeDefined();
  });
{{/members}}
{{/isUnion}}
});
