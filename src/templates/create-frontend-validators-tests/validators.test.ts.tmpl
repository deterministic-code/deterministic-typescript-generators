import { describe, it, expect } from "vitest";
import { {{&symbols}} } from "{{validatorsImport}}";

{{#schemas}}
describe("{{schemaName}} (frontend validators)", () => {
{{#cases}}
  it("{{name}}", () => {
    const value = {{&fixture}};
    expect(() => {{schemaName}}.parse(value)).{{assertion}}();
  });
{{/cases}}
});
{{/schemas}}
